# 기획 문서 검토 보고서

- 검토일: 2026-07-13
- 검토 범위: `docs` 폴더 활성 문서 (skills, _legacy 제외)
- 검토 방식: 읽기 전용 교차 검토 (활성 md 33건 전량 정독 + 와이어프레임/유저플로우 HTML 21건 표본 검사)
- 원본 문서는 일절 수정하지 않음

---

## 발견 문제 목록 (심각도순)

### [P-01] "현재 프론트 구현 기준" 문서들이 mock 전용 전제를 유지해 실원장 전환 문서와 정면 충돌

- 분류: 확정 모순
- 심각도: 높음
- 관련 문서: `05_프론트엔드_개발가이드/05_프론트엔드_구현기준.md`, `07_주문팡팡개별/07_주문팡팡_내부정책.md`, `08_유저플로우/03_주문운영_보조흐름_유저플로우.md`, `08_유저플로우/02_마켓연동_및_주문수집_유저플로우.md` ↔ `01_서비스기획/01(§1·§10)`, `03_기능명세서/03(§1·§8)`, `04_공통주문모델(§2)`, `05_실서비스_아키텍처(§18)`, `00_문서맵/02_로드맵(§3)`
- 문서 내 위치: 프론트 가이드 §1 "백엔드 없이…mock data 기준", §1 "React Query는 주문관리 핵심 데이터 로딩에 아직 사용하지 않는다", §13 "테스트 러너가 없다 / `npm test` 미정의" / 내부정책 §1 "백엔드/API가 붙기 전까지 mock…현재 사양" / 유저플로우03 §7 "현재 MVP는 백엔드 없이 mock…데모 구현" / 유저플로우02 §4 "주문수집 버튼 → mock 주문 목록 갱신"
- 문제 내용: 같은 날짜(2026-07-13)로 갱신된 서비스기획·기능명세·공통모델은 "PostgreSQL 실원장 + `/api/orders` 우선 + React Query 실API 사용"을 명시하고, 아키텍처 문서는 "Vitest와 `npm run test` 제공"을 명시하는데, 위 4개 문서는 여전히 mock-only·테스트 러너 없음 전제를 유지한다.
- 판단 이유: 동일 시점 활성 문서끼리 구현 전제(데이터 원천, 테스트 존재 여부, React Query 사용 여부)가 서로 반대다. 변경이력(2026-07-10 P1 수직 슬라이스 구현)과도 어긋난다.
- 예상 영향: 신규 개발자가 프론트 가이드를 기준으로 삼으면 실원장 경로·검증 절차(`npm run test`)를 무시할 수 있다.
- 확인이 필요한 질문: 프론트 가이드 §1·§13과 내부정책 §1은 P1 구현 이후 왜 갱신되지 않았는가? 의도적 스코프(순수 화면 문서)인가?
- 권장 판단 기준: 문서맵의 "충돌 발견 시 같은 변경에서 함께 수정" 원칙에 따라 07-10 이후 기준으로 일괄 갱신 필요.
- 확신도: 높음

### [P-02] 최우선 기준 문서인 핵심규칙에 `ON_HOLD` 상태가 없고 `CANCELED` 라벨도 불일치

- 분류: 확정 모순 / 최신본 확인 필요
- 심각도: 높음
- 관련 문서: `00_문서맵/01_주문처리_핵심규칙.md`(§1) ↔ `03_기능명세서/03(§2)`, `04_공통주문모델(§5)`, `07_내부정책(§4)`, `04_네이버커머스API_연동기준.md`(§6)
- 문서 내 위치: 핵심규칙 §1 내부 주문 단계 표(7개 상태, `CANCELED | 취소완료`)
- 문제 내용: 2026-07-12~13에 추가된 `ON_HOLD(처리보류)` 상태가 기능명세·공통모델·내부정책·UI/UX에는 모두 반영됐지만, 문서맵이 "기준 우선순위 2위"로 지정한 핵심규칙에는 없다. 또 `CANCELED` 라벨이 핵심규칙은 "취소완료", 나머지 문서는 "판매자취소"다. 네이버 연동기준 §6 내부 상태 표에도 `ON_HOLD`가 빠져 있다.
- 판단 이유: 상태 체계는 핵심규칙이 단일 기준인데 하위 문서보다 상태 수가 적다. 우선순위 규칙대로면 `ON_HOLD`가 비공식 상태가 되는 역전이 발생한다.
- 예상 영향: 상태 전이 구현·검증 시 기준 문서와 상세 문서 중 무엇을 따를지 혼선.
- 확인이 필요한 질문: 핵심규칙 v1.1이 ON_HOLD 도입(07-12) 이후 갱신 누락인지, ON_HOLD를 "핵심 단계"가 아닌 파생 상태로 보는 의도인지.
- 권장 판단 기준: 핵심규칙에 ON_HOLD 행과 `CANCELED=판매자취소` 라벨을 추가하거나, 핵심규칙에 "클레임·보류 상태는 기능명세를 따른다"는 위임 문구를 명시.
- 확신도: 높음

### [P-03] 발송대기 액션의 명칭·배치가 문서 5곳에서 서로 다름 (`발송 처리` vs `배송중 처리`)

- 분류: 확정 모순
- 심각도: 높음
- 관련 문서: `02_UI_UX_명세(§2·§5-4·§6)`, `03_기능명세(§6-3)`, `06_운영자_매뉴얼(§6)`, `07_내부정책(§5)`, `08_유저플로우/01(§2)`, `09_와이어프레임/04(§2·§3)`
- 문서 내 위치: UI/UX §6 전역 액션 "자동 배송중 처리, `배송중 처리`" + §5-4 행 액션 "`발송 처리`, `주문취소`" / 기능명세 6-3 상단 액션에 "`발송 처리`" 포함 / 운영매뉴얼 §6 상단 기능에 `배송중 처리`와 `발송 처리` 둘 다 / 내부정책 §5 READY_TO_SHIP 행 액션 "`배송중 처리`, `주문취소`" / 유저플로우01 §2 "`배송중 처리`" / 와이어프레임04 §3 "처리하기: `발송 처리` 버튼만 표시"(주문취소 없음), §2 상단 "일괄 `배송중 처리`"
- 문제 내용: 발송대기에서 (a) 행 액션 이름이 `발송 처리`인지 `배송중 처리`인지, (b) `발송 처리`가 전역 액션에도 존재하는지, (c) 행에 `주문취소`가 있는지 세 가지가 문서마다 다르다.
- 판단 이유: 같은 화면·같은 버튼에 대한 정의가 상충하며 어느 쪽이 최신인지 문서 자체로 판정할 수 없다.
- 예상 영향: UI 문구/버튼 구성 구현 및 UAT 시 기대값이 갈려 검수 실패 또는 잘못된 회귀 판정.
- 확인이 필요한 질문: 현재 코드 기준 발송대기의 전역 버튼과 행 버튼의 실제 구성은 무엇인가?
- 권장 판단 기준: 문서맵 우선순위(코드 → 핵심규칙 → …)에 따라 실제 구현 확인 후 전 문서 용어를 하나로 통일. (본 검토는 코드 접근 제외 범위라 확정 불가)
- 확신도: 높음 (불일치 자체), 판정 기준은 코드 확인 필요

### [P-04] 클레임 화면 필터 범위가 문서 간 모순 (유형만 vs 유형+상태+요청자+기한)

- 분류: 확정 모순 / 최신본 확인 필요
- 심각도: 중간
- 관련 문서: `02_UI_UX_명세(§5-9)`, `00_문서맵/99_변경이력(2026-07-12)` ↔ `03_기능명세(§6-5)`, `06_운영자_매뉴얼(§8)`, `09_와이어프레임/06(§3)`
- 문서 내 위치: UI/UX 5-9 "검색, 취소/반품/교환 유형, 공통 상태, 요청자, 기한 초과·24시간 이내 필터를 제공" ↔ 기능명세 6-5 "유형 필터는 전체/취소/반품/교환만 제공, 현재단계 필터 미제공", 운영매뉴얼 §8 "유형 필터만 제공"
- 문제 내용: 변경이력 07-12에 "검색·유형·상태·요청자·활성 기한 필터 추가"가 기록됐고 UI/UX에는 반영됐으나, 기능명세·운영매뉴얼·클레임 와이어프레임은 구버전(유형만)을 유지한다.
- 판단 이유: 변경이력이 UI/UX 쪽 서술을 뒷받침하므로 나머지가 갱신 누락으로 보인다.
- 예상 영향: 클레임 작업함 QA 체크리스트가 문서에 따라 달라짐.
- 확인이 필요한 질문: 기능명세 6-5는 demo CLAIM 화면 기준이고 UI/UX 5-9는 실클레임 작업함 기준으로, 두 화면이 별개인가? (별개라면 그 구분을 각 문서에 명시해야 함)
- 권장 판단 기준: 실클레임 작업함/데모 클레임 화면을 문서상 명확히 분리 표기.
- 확신도: 높음

### [P-05] 소싱라이프 결제완료 + 송장 미수신 처리의 이중 정책과 미확정 P0 계약

- 분류: 확인 필요 (기획 의도 정합성)
- 심각도: 높음 (정책 리스크)
- 관련 문서: `01_서비스기획(§8-1)`, `03_기능명세(§6-2)`, `06_운영자_매뉴얼(§5)` ↔ `00_문서맵/02_로드맵(§9-1)`, `03_기능명세서/04_UAT(UAT-11)`, `04_소싱라이프_연동계약(§12)`, `01_서비스기획/02_PRD(§7-4·§14)`
- 문서 내 위치: 서비스기획 8-1 "송장이 없으면 구매 완료 반영 오류로 처리" ↔ 로드맵 9-1 "결제 상태는 저장하되 구매 완료 판정 보류, 상품준비에 '송장대기' 작업 표시", UAT-11 "결제상태 저장, 발송 불가·송장대기"
- 문제 내용: 프로토타입 트랙은 송장 없는 구매완료 응답을 "오류"로, 실서비스 트랙은 "정상 결제 + 송장대기"로 처리한다. 두 트랙 분리는 문서에 명시돼 있으나, 어느 쪽이 최종 UI가 될지는 소싱라이프의 송장 선발급/후발급 계약(연동계약 §12, PRD §14의 1번 항목)이 확정돼야만 결정된다. 이 P0 결정이 07-10 이후 미해소 상태로 남아 있다.
- 판단 이유: 로드맵 G0 종료 게이트는 "미확정 정책이 P0 목록에 남아 있지 않음"인데 해당 항목이 여전히 미확정으로 표기돼 있다.
- 예상 영향: 소싱 구매 연동(P3) 착수 시 화면 정책 재작업 가능성.
- 확인이 필요한 질문: 소싱라이프가 결제 시 유효한 국내송장을 함께 반환하는가? (연동계약 §12의 6개 질문)
- 권장 판단 기준: 소싱라이프 서버 협의 결과를 근거로 상품준비 내 "송장대기" 표시 여부를 확정하고 서비스기획 8-1 문구를 정리.
- 확신도: 높음 (미확정 사실), 정책 방향은 담당자 결정 사안

### [P-06] 검색 대상 필드 정의가 3가지로 갈라짐

- 분류: 정의 불일치
- 심각도: 중간
- 관련 문서: `02_UI_UX_명세(§2·§3-2)`, `03_기능명세(§3)`, `01_서비스기획(§7-1)`, `05_프론트가이드(§4)`, `06_운영자_매뉴얼(§3)`, `08_유저플로우/02(§6)`
- 문서 내 위치: UI/UX — 8개 필드(주문번호 2종, 상품명, 구매자/수령인 명·연락처, 통관부호) / 기능명세 — 9개 필드(+내부 주문 ID, 배송메시지) / 서비스기획·운영매뉴얼·프론트가이드·유저플로우 — 4개 필드(상품명, 주문번호, 주문자, 수령인)
- 문제 내용: 같은 검색창의 검색 범위가 문서군마다 다르다. 특히 `배송메시지`, `내부 주문 ID` 검색은 기능명세에만 있다.
- 판단 이유: 검색 스코프는 테스트 가능한 기능 정의인데 단일 기준이 없다.
- 예상 영향: 검색 QA 기대값 불일치, 연락처/통관부호 검색 시 개인정보 로그 정책(SEC-002 "일반 검색 로그에 원문 금지")과의 정합 검토 누락 가능.
- 확인이 필요한 질문: 현재 구현의 실제 검색 필드는 무엇이며, placeholder 문구(4개 필드)와 실제 검색 범위(9개 필드)가 의도적으로 다른가?
- 권장 판단 기준: "placeholder 표기 필드"와 "실제 검색 대상 필드"를 구분해 한 문서에 확정하고 나머지는 참조.
- 확신도: 높음

### [P-07] 마켓 계정 인증 입력값이 프로토타입 문서와 마켓 API 문서 간 불일치

- 분류: 정의 불일치
- 심각도: 중간
- 관련 문서: `04_공통주문모델(§4)`, `09_와이어프레임/05(§6)`, `05_프론트가이드(§10)` ↔ `04_06_마켓별_매트릭스(§7)`, `04_11번가OpenAPI(§2)`, `04_ESM_Trading(§3)`, `04_쿠팡OpenAPI(§2)`
- 문서 내 위치: 프로토타입 측 — 11번가 "API Key"만, G마켓/옥션 "Master ID, Seller ID"(Secret Key 없음), 쿠팡에 "쿠팡 윙 로그인 ID" 포함 / 실서비스 측 — 11번가 "Seller ID + API Key", ESM "Master ID + Secret Key + 사이트별 Seller ID", 쿠팡 "Vendor ID, Access Key, Secret Key"(윙 로그인 ID 없음)
- 문제 내용: 마켓연동 모달 입력 필드 정의가 실서비스 인증 요구사항과 다르다. 특히 ESM은 Secret Key 없이는 JWT 생성이 불가능한데 화면 명세에 입력 필드가 없다.
- 판단 이유: 같은 기능(계정 등록)의 필수 입력값이 문서 계열 간 다르며, 실연동 시 화면 개편이 확정적으로 필요해진다.
- 예상 영향: 11번가·ESM 연동(P5) 시 마켓연동 화면·모델 재작업.
- 확인이 필요한 질문: 프로토타입 입력 필드를 실서비스 요구값으로 선제 정렬할지, P5에서 일괄 변경할지. "쿠팡 윙 로그인 ID"는 어떤 용도로 수집하는가?
- 권장 판단 기준: 매트릭스 §7(마켓 연결 입력정보)을 단일 기준으로 삼고 프로토타입 문서에 "실서비스 시 변경 예정" 주석 추가.
- 확신도: 높음

### [P-08] `DELIVERED`(배송완료) 판정 기준이 문서마다 다름 (실송장 기준 vs 마켓 상태 기준)

- 분류: 정의 불일치
- 심각도: 중간
- 관련 문서: `00_문서맵/01_핵심규칙(§1)`, `05_실서비스_아키텍처(§14)` ↔ `02_UI_UX_명세(§4)`, `03_기능명세(§2)`, `04_공통주문모델(§5)`
- 문서 내 위치: 핵심규칙 "`DELIVERED` = 실제 국내송장 기준 배송완료", 아키텍처 불변식 "DELIVERED는 실제 KOREA_DOMESTIC shipment가 DELIVERED여야" ↔ UI/UX §4 "배송완료 = 마켓 배송완료 또는 구매확정 주문", 기능명세 §2 "배송완료 또는 구매확정 상태"
- 문제 내용: 한쪽은 실물 배송 완료(택배 추적)를, 다른 쪽은 마켓 동기화 상태를 기준으로 정의한다. 직접전달 주문(마켓은 계속 배송중)은 전자 기준으로만 배송완료에 들어갈 수 있어, UI/UX §4 정의로는 배송완료 탭 진입이 설명되지 않는다. 같은 UI/UX 문서 안에서도 §5-6은 "실제 국내송장 기준 배송완료 시 배송완료 탭 이동"이라고 적어 자체 모순이다.
- 판단 이유: 두 정의가 직접전달 케이스에서 다른 결과를 낸다.
- 예상 영향: 배송완료 건수·탭 필터의 판정 로직이 구현자에 따라 달라질 수 있음.
- 확인이 필요한 질문: DELIVERED 판정은 "마켓 배송완료 ∪ 실송장 배송완료"의 합집합인가?
- 권장 판단 기준: "일반 택배는 마켓 배송완료 수집 시, 직접전달은 실송장 배송완료 시"처럼 경로별 판정 조건을 한 문장으로 명문화.
- 확신도: 중간

### [P-09] `전체` 탭 존재 여부가 문서 내·문서 간 불일치

- 분류: 정의 불일치
- 심각도: 낮음~중간
- 관련 문서: `01_서비스기획(§7 vs §9)`, `05_프론트가이드(§2 vs §3)`, `02_UI_UX_명세(§3)`, `09_와이어프레임/01(§2)`
- 문서 내 위치: 서비스기획 §7 상단 탭 목록에 `전체` 없음 ↔ 같은 문서 §9 "`전체` 탭은 처리보류를 포함…" / 프론트가이드 §3 탭 목록에 `전체` 없음 ↔ §2 라우트에 `/orders?view=all` 있음
- 문제 내용: `전체` 탭(ON_HOLD 조회 진입점)이 탭 목록 서술에서 누락돼 문서 내 모순이 있다.
- 판단 이유: 07-12 `ON_HOLD` 도입 시 탭 목록 문단 갱신 누락으로 추정.
- 예상 영향: 경미하나, 처리보류 주문의 유일한 조회 경로가 목록에서 빠져 오해 소지.
- 확인이 필요한 질문: 없음(단순 갱신 누락으로 보임).
- 권장 판단 기준: 탭 열거 문단에 `전체`를 추가.
- 확신도: 높음

### [P-10] 네이버 매핑 초안: 구매자 취소 완료(`CANCEL_DONE`)를 내부 `CANCELED`(판매자취소)로 매핑

- 분류: 기획 의도 불일치
- 심각도: 중간
- 관련 문서: `04_네이버커머스API_연동기준.md(§6.1)` ↔ `03_기능명세(§1·§2)`, `04_공통주문모델(§5)`
- 문서 내 위치: 네이버 문서 §6.1 "`productOrderStatus=CANCELED` 또는 `claimStatus=CANCEL_DONE` → `CANCELED`"
- 문제 내용: 기능명세는 `CANCELED`를 "판매자가 직접 취소 처리한 종료 상태"로, 구매자 요청 취소는 `CLAIM`으로 정의한다. 그러나 매핑 초안은 구매자 취소 완료(`CANCEL_DONE`)까지 `CANCELED`로 넣어 판매자/구매자 취소 분리 원칙과 어긋난다.
- 판단 이유: "판매자 취소 응답이나 인과 불명 claim을 근거로…연쇄 호출하지 않는다"(같은 문서 §9)는 분리 원칙과 §6.1 매핑이 상충.
- 예상 영향: 구매자 취소 완료 주문이 판매자취소 통계·화면에 섞여 클레임 이력 추적이 왜곡될 수 있음.
- 확인이 필요한 질문: 구매자 취소 완료 주문의 내부 종착 상태는 무엇인가? (`CLAIM` 종료 상태 유지 vs 별도 상태)
- 권장 판단 기준: 매핑 초안에 요청자(구매자/판매자) 구분 축을 추가해 확정. 문서 스스로 "초안"이라 명시했으므로 확정 작업 필요 항목으로 관리.
- 확신도: 중간

### [P-11] 이미지 크게 보기 "복사" 대상 불일치 (이미지 vs 이미지 주소)

- 분류: 확정 모순
- 심각도: 낮음
- 관련 문서: `02_UI_UX_명세(§5-3)` ↔ `09_와이어프레임/07(§1·§3·§5)`
- 문서 내 위치: UI/UX "이미지 복사 버튼은 현재 표시 중인 상품 이미지를 클립보드에 복사한다" ↔ 와이어프레임07 "이미지 주소를 복사", 성공 메시지 "상품 이미지 주소를 복사했습니다."
- 문제 내용: 복사 대상이 이미지 데이터인지 URL 문자열인지 문서가 상반된다.
- 예상 영향: 소싱 시 이미지 검색 활용 방식이 달라짐(붙여넣기 대상 상이). QA 기대값 불일치.
- 확인이 필요한 질문: 현재 구현이 복사하는 것은 무엇인가?
- 권장 판단 기준: 구현 확인 후 두 문서 문구 통일.
- 확신도: 높음

### [P-12] 발송대기 와이어프레임의 상품정보에 `상품주문번호` 표기 — 숨김 컬럼 정책과 충돌

- 분류: 확정 모순
- 심각도: 낮음
- 관련 문서: `09_와이어프레임/04(§3)` ↔ `02_UI_UX_명세(§5-1-1: 상품주문번호는 목록 숨김, 상품정보에는 `마켓 주문번호` 표시)`
- 문제 내용: 와이어프레임04 목록 기준은 상품정보에 "상품주문번호"를 표시한다고 적었으나 UI/UX는 상품주문번호를 목록 숨김 항목으로 규정하고 마켓 주문번호를 표시한다.
- 예상 영향: 경미(표기 오류 수준).
- 확인이 필요한 질문: 없음.
- 권장 판단 기준: `마켓 주문번호`로 정정.
- 확신도: 높음

### [P-13] 클레임 와이어프레임의 `소싱` 컬럼명 — 07-10 `상태`로 개명된 후 미반영

- 분류: 최신본 확인 필요
- 심각도: 낮음
- 관련 문서: `09_와이어프레임/06(§5)` ↔ 변경이력 2026-07-10 "`소싱` 컬럼명을 `상태`로 변경", `02_UI_UX_명세(§5)`
- 문제 내용: 06 문서는 2026-07-13자로 갱신됐음에도 목록 컬럼에 구명칭 `소싱`이 남아 있다. 또한 클레임 목록에 `선택`·`상태` 컬럼 구성이 일반 주문과 어떻게 다른지 정의가 없다.
- 권장 판단 기준: 컬럼명을 `상태`로 정정하고 클레임 목록 전용 컬럼 구성을 명시.
- 확신도: 높음

### [P-14] 내부정책의 "소싱라이프 주문번호 아이콘 링크" — 배지 링크 체계로 바뀐 뒤 잔존

- 분류: 최신본 확인 필요
- 심각도: 낮음
- 관련 문서: `07_내부정책(§6)` ↔ 변경이력 2026-07-03 "아이콘 링크에서 단계 배지 기준으로 변경", `02_UI_UX_명세(§5)`, `03_기능명세(§4-4)`
- 문제 내용: 현재 체계는 "`소싱완료` 이후 상태 배지가 주문내역 링크"인데 내부정책은 구방식(아이콘 링크)을 유지한다. `skills/기획문서_작성_스킬.md`에도 같은 구문구가 있으나 skills는 검토 제외 범위라 참고로만 남긴다.
- 권장 판단 기준: 배지 링크 기준으로 문구 교체.
- 확신도: 높음

### [P-15] 문서에 잔존한 비정상 텍스트 2건

- 분류: 이상 징후
- 심각도: 낮음
- 관련 문서: `04_공통주문모델(§14-4 말미)`, `04_07_소싱라이프_연동계약(문서 최하단)`
- 문서 내 위치:
  - ① 공통주문모델 마지막 줄 — 불릿 목록 뒤에 고아 테이블 행 `| ORD-20260617-0035 | 배송완료 | … |`이 표 밖에 떨어져 있음(§13 더미 표에 들어가야 할 행으로 추정)
  - ② 연동계약 마지막 줄 — 영어 인용문 `> This step may lead to real payment or a paid order submission, so analysis stopped before clicking.`(자동화 도구 실행 로그로 보이는 잔여물)
- 문제 내용: 렌더링이 깨지고(①), 문서 성격과 무관한 임시 텍스트(②)가 남아 있다.
- 예상 영향: 더미 0035가 검증 목록에서 누락된 것으로 오인될 수 있음.
- 권장 판단 기준: ①을 §13 표로 이동, ② 삭제.
- 확신도: 높음

### [P-16] `문의관리` 메뉴는 사이드바에 존재하나 화면·기능 명세가 전무

- 분류: 누락
- 심각도: 낮음
- 관련 문서: `01_서비스기획(§6·§7)`, `02_UI_UX_명세(§3)` ↔ 기능명세·와이어프레임 전체
- 문제 내용: 주요 메뉴 표에 "문의관리: 마켓 문의 확인 및 응대"가 있으나 라우트, 화면 구성, 상태 정의가 어떤 문서에도 없다. PRD는 문의관리를 P1로 분류한다.
- 확인이 필요한 질문: 현재 프로토타입에 문의관리 메뉴가 실제로 노출되는가? 노출된다면 빈 화면 처리 기준은?
- 권장 판단 기준: "P1 예정, 현재 메뉴 노출 여부"를 서비스기획에 한 줄 명시.
- 확신도: 중간

### [P-17] 주문처리 유저플로우가 판매자취소 진입을 신규주문에서만 표현

- 분류: 누락
- 심각도: 낮음
- 관련 문서: `08_유저플로우/01(§1·§2)` ↔ `02_UI_UX_명세(§5-6)`, `04_네이버커머스API(§9)`, `04_공통주문모델(§14-4)`
- 문제 내용: 정책상 `주문취소`는 신규주문·상품준비·발송대기에서 가능하지만, 유저플로우 01의 다이어그램·표는 `NEW`에서만 취소(→처리보류) 경로를 그린다.
- 예상 영향: 플로우 문서만 보면 상품준비/발송대기 취소 → ON_HOLD 경로가 없는 것으로 오해.
- 권장 판단 기준: §2 표에 PREPARING/READY_TO_SHIP의 `주문취소` 행 추가.
- 확신도: 높음

### [P-18] `구매확정 대기`라는 표시값이 서비스기획에만 등장

- 분류: 모호함
- 심각도: 낮음
- 관련 문서: `01_서비스기획(§8-1)` ↔ `02_UI_UX_명세(§5-6)`, `04_공통주문모델(§13 더미 0038)`
- 문서 내 위치: 서비스기획 8-1 "마켓 상태는 `배송중 · 직접전달` 또는 `구매확정 대기`로 별도 표시한다"
- 문제 내용: 다른 문서는 구매확정 전까지 `마켓 배송중 · 직접전달` 배지 유지만 규정하며 `구매확정 대기`라는 별도 배지는 정의하지 않는다. 실제 배지인지 서술적 표현인지 불명확.
- 권장 판단 기준: 배지 명세에 존재하지 않는 값이면 서술을 `마켓 배송중 · 직접전달 유지`로 통일.
- 확신도: 중간

### [P-19] 네이버 택배사 코드 예시의 정확성 확인 필요 (롯데=`HYUNDAI`, 로젠=`KGB`)

- 분류: 확인 필요
- 심각도: 낮음
- 관련 문서: `04_네이버커머스API_연동기준.md(§8.1)`
- 문제 내용: 롯데택배→`HYUNDAI`, 로젠택배→`KGB`는 과거 사명 기반 코드로 추정되는데, 실제 네이버 물류사 코드와 일치하는지 공식 `GET /v1/logistics/logistics-companies` 결과로 검증되지 않았다. 문서 스스로 "예시이며 별도 매핑 테이블 관리"라고 명시해 치명적이지는 않다.
- 권장 판단 기준: 매핑 테이블 확정 시(§15의 3번 항목) 실 API 응답을 근거로 확정.
- 확신도: 낮음

---

## 최종 보고

- **검토한 문서 수**: 활성 md 33건 전량 정독 + 와이어프레임/유저플로우 HTML 21건 표본 검사 (skills·_legacy 제외, 네이버 공식링크 인덱스는 링크 목록 성격으로 표기 검사만)
- **발견한 문제 수**: 19건
- **확정 모순**: 6건 (P-01, P-02, P-03, P-04, P-11, P-12)
- **확인 필요 항목**: 7건 (P-05, P-08, P-10, P-13, P-14, P-16, P-19 — 최신본 확인·계약 확정 포함)
- **심각도 높음**: 3건
  - P-01 현재기준 문서군의 mock-only 전제 vs 실원장 전환 (문서 신뢰도 자체를 훼손)
  - P-02 핵심규칙(최우선 기준)에 ON_HOLD 부재·CANCELED 라벨 불일치
  - P-03 발송대기 액션 명칭·배치 5개 문서 불일치

### 담당자에게 확인해야 할 질문 (우선순위순)

1. 발송대기 화면의 실제 버튼 구성(전역/행, `발송 처리` vs `배송중 처리`, 행 `주문취소` 유무)은 현재 코드 기준 무엇인가? → P-03 정리 기준
2. 소싱라이프는 결제 시 국내송장을 선발급하는가, 후발급하는가? (연동계약 §12의 6개 질문) → P-05, 상품준비 "송장대기" UI 확정의 선행조건
3. 핵심규칙 v1.1에 ON_HOLD를 추가할 것인가, 클레임·보류 상태를 기능명세로 위임할 것인가? → P-02
4. 프론트 가이드·내부정책·유저플로우 02/03의 mock-only 서술은 갱신 누락인가, 의도적 데모 문서인가? → P-01
5. 구매자 취소 완료(`CANCEL_DONE`) 주문의 내부 종착 상태는? → P-10
6. 검색창의 실제 검색 필드 범위와 placeholder 표기를 어떻게 확정할 것인가? → P-06
7. 11번가 Seller ID, ESM Secret Key 입력 필드를 프로토타입 화면에 선반영할 것인가? → P-07
